<Page
    x:Class="WindowsControl.ScanNetwork"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WindowsControl"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <RelativePanel>
            <TextBlock RelativePanel.AlignHorizontalCenterWithPanel="True" Name="IPField">Local IP-Adresse:</TextBlock>
            <TextBlock Name="IPUI" Text="{Binding LocalIP}" RelativePanel.AlignHorizontalCenterWithPanel="True" RelativePanel.RightOf="IPField"></TextBlock>
            <ProgressRing Name="IPProgress" IsActive="True" RelativePanel.Below="IPUI" RelativePanel.AlignHorizontalCenterWithPanel="True"></ProgressRing>
            <TextBlock RelativePanel.Below="IPProgress"  RelativePanel.AlignHorizontalCenterWithPanel="True">Searching</TextBlock>

            <ListBox ItemsSource="{Binding ESPResults}" RelativePanel.Below="IPProgress"  RelativePanel.AlignHorizontalCenterWithPanel="True" Margin="0,50,0,0" Name="IPResults"></ListBox>
        </RelativePanel>
    </Grid>
</Page>
